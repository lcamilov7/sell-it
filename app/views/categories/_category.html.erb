<div id="<%= dom_id category %>">
  <%= category.name %>
  <%= link_to 'Edit', edit_category_path(category) %>
  <%= link_to 'Delete', category_path(category), data: {turbo_method: :delete} %>
</div>
